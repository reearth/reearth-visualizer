import { Meta } from "@storybook/react";
import React from "react";

import Divider from ".";

export default {
  title: "atoms/Divider",
  component: Divider,
} as Meta;

export const Default = () => <Divider />;
