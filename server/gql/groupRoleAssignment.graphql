type GroupRoleAssignment {
  id: ID!
  userId: ID!
  roleIds: [ID!]!
  groupIds: [ID!]!
}

input AddGroupRoleAssignmentInput {
  userId: ID!
  roleIds: [ID!]!
  groupIds: [ID!]!
}

type AddGroupRoleAssignmentPayload {
  GroupRoleAssignment: GroupRoleAssignment!
}

extend type Mutation {
  addGroupRoleAssignment(input: AddGroupRoleAssignmentInput!): AddGroupRoleAssignmentPayload
}
