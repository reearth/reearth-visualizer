import { FC } from "react";

import { Panel } from "@reearth/beta/ui/layout";

const ToolsPanel: FC = () => {
  return <Panel storageId="editor-map-tools-panel" extend />;
};

export default ToolsPanel;
